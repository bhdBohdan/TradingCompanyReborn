<Window x:Class="TradingCompany.WPF2.Windows.HomePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:TradingCompany.WPF2.Converters"
        mc:Ignorable="d"
        Title="Trading Company" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        FontFamily="Segoe UI">

    <Window.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibility" />

        <Style x:Key="AvatarStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="StrokeThickness" Value="1"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Fill" Value="#EEEEEE"/>
        </Style>

        <SolidColorBrush x:Key="PrimaryColor" Color="#4A90E2"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#357ABD"/>
        <SolidColorBrush x:Key="SecondaryColor" Color="#2C3E50"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
        <SolidColorBrush x:Key="LightBorderBrush" Color="#DDDDDD"/>

        <Style x:Key="ModernBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineBtn" TargetType="Button" BasedOn="{StaticResource ModernBtn}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EBF5FB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,10,10,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                SnapsToDevicePixels="true"
                                CornerRadius="4">
                            <GridViewRowPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E8F0FE"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#D2E3FC"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Trading Company" FontSize="26" FontWeight="Bold" Foreground="{StaticResource SecondaryColor}"/>
                <TextBlock Text=" | Home" FontSize="26" FontWeight="Light" Foreground="#95A5A6" Margin="5,0,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">

                <Button Click="ProfileButton_Click" Style="{StaticResource OutlineBtn}"
        Visibility="{Binding CurrentUser, Converter={StaticResource NullToVisibility}}"
        BorderThickness="0">
                    <StackPanel Orientation="Horizontal">

                        <Ellipse Style="{StaticResource AvatarStyle}">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>

                        <TextBlock Text="{Binding CurrentUser.Username}" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
                    </StackPanel>
                </Button>

                <Button Content="Login" Click="LoginButton_Click" 
                        Style="{StaticResource ModernBtn}" Width="120"
                        Visibility="{Binding CurrentUser, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <Button Content="+ Add Product" Click="AddProductButton_Click"
                        Style="{StaticResource ModernBtn}"
                        Visibility="{Binding CurrentUser, Converter={StaticResource NullToVisibility}}"/>

                <TextBlock Text="Please login to view and manage products."
                           VerticalAlignment="Center" Foreground="#7F8C8D" FontStyle="Italic"
                           Visibility="{Binding CurrentUser, Converter={StaticResource NullToVisibility}, ConverterParameter=Invert}"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10" Effect="{DynamicResource {x:Static DropShadowEffect.ShadowDepthProperty}}">
            

            <ListView x:Name="ProductsListView" 
                      ItemsSource="{Binding ProductsView}"
                      SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                      MouseDoubleClick="ProductsListView_MouseDoubleClick" 
                      Visibility="{Binding DataContext.CurrentUser, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource NullToVisibility}}"
                      BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="PRODUCT" DisplayMemberBinding="{Binding ProductName}" Width="250"/>
                        <GridViewColumn Header="PRICE" DisplayMemberBinding="{Binding Price, StringFormat=c}" Width="100"/>
                        <GridViewColumn Header="CATEGORY" DisplayMemberBinding="{Binding Category}" Width="150"/>
                        <GridViewColumn Header="SELLER" DisplayMemberBinding="{Binding User.Username}" Width="150"/>
                        <GridViewColumn Header="DATE" DisplayMemberBinding="{Binding CreatedDate, StringFormat=d}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0"
                    Visibility="{Binding CurrentUser, Converter={StaticResource NullToVisibility}}">
            <TextBlock Text="Admin Actions:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#95A5A6" FontSize="12"/>

            <Button Content="Grant Admin" Click="GiveAdminRoleButton_Click"
                    Style="{StaticResource OutlineBtn}" Margin="5,0" FontSize="12" Padding="10,5"/>

            <Button Content="Revoke Admin" Click="RemoveAdminRoleButton_Click"
                    Style="{StaticResource OutlineBtn}" Margin="5,0" FontSize="12" Padding="10,5"
                    Foreground="#E74C3C" BorderBrush="#E74C3C"/>
        </StackPanel>

    </Grid>
</Window>