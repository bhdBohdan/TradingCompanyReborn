<Window x:Class="TradingCompany.WPF2.Windows.ProductDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Product Details" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F0F2F5"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Use standard BooleanToVisibilityConverter (or your custom one) -->
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <!-- COLORS -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#4A90E2"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#357ABD"/>
        <SolidColorBrush x:Key="DangerColor" Color="#E74C3C"/>
        <SolidColorBrush x:Key="DangerDarkColor" Color="#C0392B"/>
        <SolidColorBrush x:Key="TextGray" Color="#7F8C8D"/>

        <!-- FORM LABEL STYLE -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <!-- FORM INPUT (TEXTBOX) STYLE -->
        <Style x:Key="FormInput" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" CornerRadius="4" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter TargetName="border" Property="Background" Value="#FDFEFE"/>
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource DangerColor}"/>
                            </Trigger>
                            <!-- Visual change when ReadOnly (View Only mode) -->
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F2F4F4"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Foreground" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- BUTTON BASE STYLE -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PRIMARY BUTTON (Save) -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DANGER BUTTON (Delete) -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- STANDARD BUTTON (Cancel) -->
        <Style x:Key="StandardButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#333"/>
                    <Setter Property="Background" Value="#E5E7E9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Product Details" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="View or edit product information." FontSize="14" Foreground="#95A5A6" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="25">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#DDDDDD" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Style="{StaticResource FormLabel}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,15"
                         Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Category" Style="{StaticResource FormLabel}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FormInput}" Margin="0,0,0,15"
                         Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                         IsReadOnly="{Binding CanEdit, Converter={StaticResource BoolToVis}, ConverterParameter=invert}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Price" Style="{StaticResource FormLabel}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource FormInput}" Width="120" HorizontalAlignment="Left" Margin="0,0,0,15"
                         Text="{Binding Price, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Description" Style="{StaticResource FormLabel}" VerticalAlignment="Top" Margin="0,10,15,0"/>
                <TextBox Grid.Row="3" Grid.Column="1" Style="{StaticResource FormInput}"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="{Binding CanEdit, Converter={StaticResource BoolToVis}, ConverterParameter=invert}" />

                <Border Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                        Margin="0,15,0,0" Background="#FDEDEC" CornerRadius="4" BorderBrush="#F1948A" BorderThickness="1">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Error}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Error}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Foreground="{StaticResource DangerColor}" Text="{Binding Error}" Padding="10" TextWrapping="Wrap" />
                </Border>

                <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">

                    <Button Content="Delete Product" Command="{Binding DeleteCommand}" 
                            Visibility="{Binding CanDelete, Converter={StaticResource BoolToVis}}" 
                            Style="{StaticResource DangerButton}" Margin="0,0,10,0"/>

                    <Button Content="Cancel" Command="{Binding CancelCommand}" 
                            Style="{StaticResource StandardButton}" Margin="0,0,10,0"/>

                    <Button Content="Save Changes" Command="{Binding SaveCommand}" 
                            IsEnabled="{Binding CanEdit}" 
                            Style="{StaticResource PrimaryButton}" Width="120"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</Window>