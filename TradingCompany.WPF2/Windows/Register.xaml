<Window x:Class="TradingCompany.WPF2.Windows.Register"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:local="clr-namespace:TradingCompany.WPF2.Windows"
          mc:Ignorable="d"
          Title="Register" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Email" Grid.Row="0" />
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Margin="0,18,0,0" />

        <TextBlock Text="Username" Grid.Row="1" Margin="0,10,0,0" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="0,28,0,0" />

        <TextBlock Text="Password" Grid.Row="2" Margin="0,10,0,0" />
        <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Grid.Row="2" Margin="0,28,0,0" />

        <TextBlock Text="Confirm Password" Grid.Row="3" Margin="0,10,0,0" />
        <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Grid.Row="3" Margin="0,28,0,0" />

        <TextBlock Text="{Binding RegisterError}" Foreground="Red" Grid.Row="4" Margin="0,10,0,0" />

        <StackPanel Orientation="Horizontal" Grid.Row="5" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Command="{Binding CloseCommand}" Margin="0,0,10,0" />
            <Button Content="Login" Click="LoginButton_Click" Margin="0,0,10,0" />
            <Button Content="Register" Command="{Binding RegisterCommand}" />
        </StackPanel>
    </Grid>
</Window>
