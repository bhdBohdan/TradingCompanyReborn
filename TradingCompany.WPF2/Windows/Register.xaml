<Window x:Class="TradingCompany.WPF2.Windows.Register"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Create Account" Height="500" Width="750"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#4A90E2"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#357ABD"/>
        <SolidColorBrush x:Key="TextGray" Color="#7F8C8D"/>

        <Style TargetType="{x:Type Control}" x:Key="InputStyle">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Control">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RegisterBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#4A90E2" Offset="0"/>
                    <GradientStop Color="#8E44AD" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="TC" FontSize="72" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Opacity="0.9"/>
                <TextBlock Text="Trading Company" FontSize="20" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Text="Join our platform today." FontSize="14" Foreground="#DDD" HorizontalAlignment="Center" Margin="0,5,0,0" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="118*"/>
                <ColumnDefinition Width="77*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Create Account" FontSize="28" FontWeight="Bold" Foreground="#333" Margin="0,-30,0,40" Grid.ColumnSpan="2"/>

            <StackPanel VerticalAlignment="Top" Margin="0,19,0,0" Grid.ColumnSpan="2" Grid.RowSpan="2">

                <TextBlock Text="EMAIL ADDRESS" FontSize="10" FontWeight="Bold" Foreground="#AAA" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputStyle}" Margin="0,0,0,20"/>

                <TextBlock Text="USERNAME" FontSize="10" FontWeight="Bold" Foreground="#AAA" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource InputStyle}" Margin="0,0,0,20"/>

                <TextBlock Text="PASSWORD" FontSize="10" FontWeight="Bold" Foreground="#AAA" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged"
                             Style="{StaticResource InputStyle}" Margin="0,0,0,20"/>

                <TextBlock Text="CONFIRM PASSWORD" FontSize="10" FontWeight="Bold" Foreground="#AAA" Margin="0,0,0,5"/>
                <PasswordBox x:Name="ConfirmPasswordBox" PasswordChanged="ConfirmPasswordBox_PasswordChanged"
                             Style="{StaticResource InputStyle}" Margin="0,0,0,10"/>

                <StackPanel>
                    <TextBlock Text="{Binding Error}" Foreground="#E74C3C" TextWrapping="Wrap" FontSize="12"/>
                    <TextBlock Text="{Binding RegisterError}" Foreground="#E74C3C" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="0,287,0,0" Grid.ColumnSpan="2" Grid.RowSpan="2">
                <Button Content="REGISTER NOW" Command="{Binding RegisterCommand}" Style="{StaticResource RegisterBtn}" Height="45"/>
                <Grid Height="25">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="Cancel" Command="{Binding CloseCommand}" Style="{StaticResource LinkBtn}" HorizontalAlignment="Center" Margin="0,3,0,-3"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Already a member?" Foreground="#AAA" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <Button Content="Log In" Click="LoginButton_Click" Style="{StaticResource LinkBtn}" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>